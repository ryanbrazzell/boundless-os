# API Configuration
# Leave empty in production to use relative URLs (proxied via _redirects)
# Set to worker URL in development to connect directly to local worker
VITE_API_URL=http://localhost:8788
